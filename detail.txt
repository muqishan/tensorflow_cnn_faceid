人脸数据集因隐私问题不方便上传，使用cv_get_face.py摄像头采集人脸照片。
训练集推荐每人400-600，测试数据集推荐每人100-200。
5-10人，模型配置训练结果识别率在98%
全局配置文件setting中更改自定义配置
face_train.py 模型配置文件
remobe_bg.py内嵌方法，先执行remove_b，将背景擦出，有一些人脸信息
经过二次判断无效，人工删除，在执行remove_border，删除冗余背景进行降
噪处理。
load_dataset.py用于将训练集或测试机加载到内存，并将所有图片规格为指定尺寸。
训练模型，测试模型，本配置对10个人的识别率在98%。
